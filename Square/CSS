body {
  background: #111;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 20px;
  font-family: Arial;
}

#game {
  background: #222;
  border: 3px solid #fff;
}

.touch-pad {
  margin-top: 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  align-items: center;
}

.pad-middle {
  display: flex;
  gap: 10px;
}

.pad-btn {
  width: 60px;
  height: 60px;
  font-size: 26px;
  border-radius: 10px;
  border: none;
  background: #333;
  color: white;
  cursor: pointer;

  /* evita seleção e zoom */
  user-select: none;
  touch-action: manipulation;
}
